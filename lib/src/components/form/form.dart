import 'package:flutter/material.dart';

class FlutterDynamicForm extends StatelessWidget {
  const FlutterDynamicForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}